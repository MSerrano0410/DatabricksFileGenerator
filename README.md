# DatabricksFileGenerator
A small script to auto-generate CSV files for testing other Databricks workbooks. This currently creates .csv files, but will be expanded in the future to include .txt files.
